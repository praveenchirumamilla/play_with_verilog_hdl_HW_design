module top_module ( 
    input [15:0] a, b,
    input cin,
    output cout,
    output [15:0] sum );
    
    wire [3:0] carry;
    
    assign carry[0] = cin;
    assign cout = carry[3];
    bcd_fadd inst[3:0](.a(a), .b(b), .cin(carry), .cout(carry), .sum(sum));

endmodule
